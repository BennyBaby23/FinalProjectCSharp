@model IEnumerable<FinalProject1.Models.BuyerInfo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BuyerID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BuyerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BuyerAge)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BuyerAddress)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BuyerID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BuyerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BuyerAge)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BuyerAddress)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BuyerID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BuyerID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BuyerID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
